# pytest.ini

[pytest]
minversion = 6.0
addopts = -q --tb=short -m "not slow"
testpaths =
    tests
pythonpath =
    ..
markers =
    slow: exhaustive or long-running model-combination tests excluded from the default test run
